fileID = fopen('testfile.txt','r');

formatSpec = '%d %d %d';
A = fscanf(fileID,formatSpec);
A1 = reshape(A, 3, [])';

fclose(fileID);


figure("Color", "white");

for k = 1:size(A1, 2)
    quality = A1(k, 1)
end